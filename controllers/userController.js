const { User } = require('../models');

const userController = {
    //get all users

    // get single user by id

    // create a new user

    // update a user

    // delete user (BONUS: and delte associated thoughts)

    //add friend to friend list 

    //remove friend from friend list
};

module.exports = userController;